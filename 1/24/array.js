const numbers = [10, 20, 30, 40, 50];

const length = numbers.length;
console.log(length); // 5

const length1 = numbers[0];
console.log(length1); // 10

const length2 = numbers[numbers.length - 1];
console.log(length2); // 50
