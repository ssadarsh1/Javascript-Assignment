// null and undefined

let a = null;
let b = undefined;
console.log(a == b); // true because null and undefined are considered as equal in value
console.log(a === b); // false because null and undefined are different in both type and value
